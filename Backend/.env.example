# Configuración del Servidor
PORT=3000
NODE_ENV=development

# Configuración de Base de Datos PostgreSQL (Development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=residence_management7
DB_USER=postgres
DB_PASSWORD=postgres

# Configuración de Base de Datos PostgreSQL (Production)
# En producción, Render proporciona DATABASE_URL automáticamente
# DATABASE_URL=postgres://user:password@host:port/database

# JWT Secret
# ⚠️ IMPORTANTE: Cambia esto en producción
# Puedes generar uno con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=tu_secreto_super_seguro_cambialo_en_produccion

# CORS
# En desarrollo
CORS_ORIGIN=http://localhost:4200
# En producción
# CORS_ORIGIN=https://tu-dominio-frontend.vercel.app
